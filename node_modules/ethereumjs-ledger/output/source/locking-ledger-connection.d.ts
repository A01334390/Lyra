import { comm as LedgerConnection, eth as LedgerEthereumApi } from "ledgerco";
import { LedgerConnectionFactory } from "./connection-factories";
export declare class LockingLedgerConnection {
    private readonly lock;
    private connection;
    private ledgerConnectionFactory;
    constructor(connectionFactory: LedgerConnectionFactory);
    acquire: () => Promise<LedgerConnection>;
    release: () => Promise<void>;
    safelyCallEthereumApi: <T>(func: (ledgerEthereumApi: LedgerEthereumApi) => Promise<T>) => Promise<T>;
}
